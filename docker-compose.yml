services:
  app:
    build: .
    env_file:
      - .env
    restart: on-failure
    
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    restart: on-failure
